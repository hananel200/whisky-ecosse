{% extends 'base.html.twig' %}

{% block body %}

    {{ form_start(form) }}
    {{ form_widget(form) }}
    {{ form_end(form) }}
    <a href="{{ path('home') }}">je n'ai plus d'images à ajouter</a>
{% endblock %}

